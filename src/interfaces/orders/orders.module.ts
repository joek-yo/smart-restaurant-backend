// src/interfaces/orders/orders.module.ts

/**
 * ⚠️ DEPRECATED MODULE
 * Orders has been migrated to: src/modules/orders
 * * This file is now empty and should be deleted once 
 * all references are confirmed to be removed.
 */

import { Module } from '@nestjs/common';

@Module({
  imports: [],
})
export class OrdersModule {}